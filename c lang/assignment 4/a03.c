#include <stdio.h>

int  main(void) {

	int a;
	float b;
	char c;
	printf(" size of int is %d\n",sizeof(a));
	printf(" size of float is %d\n",sizeof(b));
	printf(" size of char is %d\n",sizeof(c));
	return 0;
}